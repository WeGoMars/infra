# 1. Python 3.11 기반 경량 이미지 사용
FROM python:3.11-slim

# 2. 작업 디렉토리 설정
WORKDIR /app

# 3. build 폴더의 전체 파일 복사
COPY build/ .

# 4. 필요 시 requirements.txt가 존재한다면 설치 (선택적 단계)
RUN pip install --no-cache-dir -r requirements.txt

# 5. 실행 명령: batch.py 실행
CMD ["python", "batch.py"]
